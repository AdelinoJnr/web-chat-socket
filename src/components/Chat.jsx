import React, { useEffect, useState } from 'react'

export default function Chat() {
    const [chatMessage, setChatMessage] = useState();

    useEffect(() => {
        // chamada para api, buscar mensagens no banco
    }, []);

    return (
        <div>

        </div>
    )
}
